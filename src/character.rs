use crate::*;

#[derive(Default)]
pub struct Character {
    pub animal: Animal,
    pub number_of_animal_legs: i32,
    pub animal_name: String,
    pub habitat: String,
    pub favourite_food: String,
}
